import { View } from 'react-native';

export default function Divider() {
  return (
    <View
      style={{
        height: 2,
        backgroundColor: '#EDEFEE',
        width: '100%',
        marginTop: 24,
      }}
    />
  );
}